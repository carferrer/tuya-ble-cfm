target-version = "py314"

[lint]
# Start with correctness-focused rules so CI catches real errors without
# forcing a repository-wide style rewrite in the same PR.
select = ["E9", "F63", "F7", "F82"]
